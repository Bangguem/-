<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>게시판</title>
</head>
<body>
    <h1>게시판</h1>

    <!-- 게시글 추가 버튼 -->
    <a href="/boards/new"><button>게시글 추가</button></a>

    <!-- 게시글 목록 -->
    <ul>
        <% posts.forEach(post => { %>
            <li>
                <h2><%= post.title %></h2>
                <p><%= post.content %></p>
                <p>작성자: <%= post.author %></p>
                <p>작성일: <%= new Date(post.createdAt).toLocaleString() %></p>
                <!-- 수정 버튼 -->
                <a href="/boards/<%= post._id %>/edit"><button>수정</button></a>
                 <!-- 삭제 버튼 -->
                 <form action="/boards/<%= post._id %>?_method=DELETE" method="POST" style="display:inline;">
                    <button type="submit">삭제</button>
                </form>
            </li>
        <% }) %>
    </ul>

    <a href="/">홈으로 이동</a>
</body>
</html>